# Copy to Deploy/.env (gitignored) and fill in.
# API key from https://www.nuget.org/account/apikeys — scope it to Tailcat.Link.
# The key must have "Push" and, for the first release, "Push new packages and
# package versions", with a glob that matches Tailcat.Link.
NUGET_API_KEY=
